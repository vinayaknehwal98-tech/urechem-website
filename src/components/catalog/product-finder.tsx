"use client";
import { useMemo, useState } from "react";
import { Search } from "lucide-react";
import { productFamilies } from "@/data/catalog";
import { FamilyCard } from "./cards";
export function ProductFinder(){const [q,setQ]=useState(""); const [fam,setFam]=useState("all"); const filtered=useMemo(()=>productFamilies.filter(f=>(fam==="all"||f.slug===fam)&&`${f.name} ${f.description} ${f.products.map(p=>p.name).join(" ")}`.toLowerCase().includes(q.toLowerCase())),[q,fam]); return <section className="space-y-6"><div className="grid gap-3 rounded-lg border border-white/10 bg-navy-900 p-4 md:grid-cols-[1fr_260px]"><label className="relative block"><span className="sr-only">Search products</span><Search className="pointer-events-none absolute left-3 top-3 size-5 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search product names, families or applications" className="w-full rounded-md border border-white/10 bg-white/5 py-3 pl-11 pr-3 text-white outline-none focus:border-cyan-300"/></label><label><span className="sr-only">Filter by family</span><select value={fam} onChange={e=>setFam(e.target.value)} className="w-full rounded-md border border-white/10 bg-navy-800 p-3 text-white"><option value="all">All families</option>{productFamilies.map(f=><option key={f.slug} value={f.slug}>{f.name}</option>)}</select></label></div>{filtered.length ? <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(f=><FamilyCard key={f.slug} family={f}/>)}</div> : <div className="rounded-lg border border-dashed border-white/20 p-8 text-center text-slate-300">No matching product families. Try a broader term or contact Urechem for guided discovery.</div>}</section>}
