import Link from "next/link";import { Container } from "@/components/ui/container";import { applications } from "@/data/catalog";import { FamilyLinks } from "@/components/catalog/cards";export const metadata={title:"Applications | Urechem"};export default function Page(){return <Container className="py-16"><h1 className="text-4xl font-semibold">Application-led discovery</h1><p className="mt-4 max-w-3xl text-slate-300">Start from the manufacturing or performance problem, then escalate to expert validation.</p><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{applications.map(a=><Link key={a.slug} href={`/applications/${a.slug}`} className="rounded-lg border border-white/10 bg-white/[0.04] p-5 hover:border-cyan-300"><h2 className="text-xl font-semibold">{a.name}</h2><p className="mt-2 text-sm text-slate-300">{a.summary}</p><div className="mt-4"><FamilyLinks slugs={a.familySlugs}/></div></Link>)}</div></Container>}
