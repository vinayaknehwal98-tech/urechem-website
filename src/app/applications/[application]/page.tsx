import { notFound } from "next/navigation";import { Container } from "@/components/ui/container";import { applications,getApplication } from "@/data/catalog";import { FamilyLinks, IndustryLinks, ValidationNote } from "@/components/catalog/cards";export function generateStaticParams(){return applications.map(a=>({application:a.slug}))}export default async function Page({params}:{params:Promise<{application:string}>}){const {application}=await params;const a=getApplication(application);if(!a)notFound();return <Container className="py-16"><h1 className="text-4xl font-semibold">{a.name}</h1><p className="mt-4 max-w-3xl text-slate-300">{a.summary}</p><div className="mt-6"><ValidationNote/></div><div className="mt-10 grid gap-6 md:grid-cols-3"><section className="rounded-lg border border-white/10 p-5"><h2 className="font-semibold">Common inputs</h2><ul className="mt-3 space-y-2 text-sm text-slate-300">{a.needs.map(n=><li key={n}>• {n}</li>)}</ul></section><section className="rounded-lg border border-white/10 p-5 md:col-span-2"><h2 className="font-semibold">Relevant families</h2><div className="mt-3"><FamilyLinks slugs={a.familySlugs}/></div><h2 className="mt-6 font-semibold">Related industries</h2><div className="mt-3"><IndustryLinks slugs={a.industrySlugs}/></div></section></div></Container>}
