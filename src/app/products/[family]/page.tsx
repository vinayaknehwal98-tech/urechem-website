import { notFound } from "next/navigation";import Link from "next/link";import { Container } from "@/components/ui/container";import { getFamily, productFamilies, relatedFamilies } from "@/data/catalog";import { AppLinks, DocumentStatus, ValidationNote } from "@/components/catalog/cards";export function generateStaticParams(){return productFamilies.map(f=>({family:f.slug}))}export async function generateMetadata({params}:{params:Promise<{family:string}>}){const {family}=await params;const f=getFamily(family);return {title:f?`${f.name} | Urechem Products`:"Product family"}}export default async function Page({params}:{params:Promise<{family:string}>}){const {family}=await params;const f=getFamily(family);if(!f)notFound();return <Container className="py-16"><p className="font-mono text-sm uppercase tracking-[0.25em] text-cyan-300">{f.shortName}</p><h1 className="mt-4 text-4xl font-semibold">{f.name}</h1><p className="mt-4 max-w-3xl text-slate-300">{f.description}</p><div className="mt-6"><ValidationNote/></div><section className="mt-10 grid gap-4 md:grid-cols-2">{f.products.map(p=><Link key={p.slug} href={`/products/${f.slug}/${p.slug}`} className="rounded-lg border border-white/10 bg-white/[0.04] p-5 hover:border-cyan-300"><h2 className="text-xl font-semibold">{p.name}</h2><p className="mt-2 text-sm text-slate-300">{p.description}</p></Link>)}</section><section className="mt-10 grid gap-6 md:grid-cols-2"><DocumentStatus/><div className="rounded-lg border border-white/10 bg-navy-900 p-4"><h3 className="font-semibold">Relevant applications</h3><div className="mt-3"><AppLinks slugs={f.applications}/></div></div></section><h2 className="mt-12 text-2xl font-semibold">Related families</h2><div className="mt-4 flex flex-wrap gap-3">{relatedFamilies(f.slug).map(r=><Link className="rounded-md border border-white/10 px-4 py-2 text-cyan-100" key={r.slug} href={`/products/${r.slug}`}>{r.name}</Link>)}</div></Container>}
