import { notFound } from "next/navigation";import { Container } from "@/components/ui/container";import { industries,getIndustry } from "@/data/catalog";import { AppLinks, FamilyLinks, ValidationNote } from "@/components/catalog/cards";export function generateStaticParams(){return industries.map(i=>({industry:i.slug}))}export default async function Page({params}:{params:Promise<{industry:string}>}){const {industry}=await params;const i=getIndustry(industry);if(!i)notFound();return <Container className="py-16"><h1 className="text-4xl font-semibold">{i.name}</h1><p className="mt-4 max-w-3xl text-slate-300">{i.summary}</p><div className="mt-6"><ValidationNote/></div><div className="mt-10 grid gap-6 md:grid-cols-3"><section className="rounded-lg border border-white/10 p-5"><h2 className="font-semibold">Common needs</h2><ul className="mt-3 space-y-2 text-sm text-slate-300">{i.needs.map(n=><li key={n}>• {n}</li>)}</ul></section><section className="rounded-lg border border-white/10 p-5 md:col-span-2"><h2 className="font-semibold">Applications</h2><div className="mt-3"><AppLinks slugs={i.applicationSlugs}/></div><h2 className="mt-6 font-semibold">Commonly evaluated families</h2><div className="mt-3"><FamilyLinks slugs={i.familySlugs}/></div></section></div></Container>}
